import socket

SBOX = [
    
]

INV_SBOX = [0] * 256
for i in range(256):
    INV_SBOX[SBOX[i]] = i

RCON = [0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1B, 0x36]

def hex_to_bytes(h):
    return [int(h[i:i+2], 16) for i in range(0, len(h), 2)]

def bytes_to_hex(b):
    return ''.join(f"{x:02X}" for x in b)

def key_expansion(key):
    key = list(key)
    expanded = key[:]
    for i in range(10):
        temp = expanded[-4:]
        temp = temp[1:] + temp[:1]
        temp = [SBOX[b] for b in temp]
        temp[0] ^= RCON[i]
        for j in range(4):
            temp[j] ^= expanded[-16 + j]
        expanded += temp
        for j in range(12):
            expanded.append(expanded[-16] ^ expanded[-4])
    return expanded

def add_round_key(s, k):
    return [a ^ b for a, b in zip(s, k)]

def inv_sub_bytes(s):
    return [INV_SBOX[b] for b in s]

def inv_shift_rows(s):
    return [
        s[0], s[13], s[10], s[7],
        s[4], s[1], s[14], s[11],
        s[8], s[5], s[2], s[15],
        s[12], s[9], s[6], s[3]
    ]

def mul(a, b):
    p = 0
    for _ in range(8):
        if b & 1:
            p ^= a
        hi = a & 0x80
        a = (a << 1) & 0xFF
        if hi:
            a ^= 0x1B
        b >>= 1
    return p

def inv_mix_columns(s):
    out = []
    for i in range(0, 16, 4):
        a = s[i:i+4]
        out += [
            mul(a[0], 14) ^ mul(a[1], 11) ^ mul(a[2], 13) ^ mul(a[3], 9),
            mul(a[0], 9) ^ mul(a[1], 14) ^ mul(a[2], 11) ^ mul(a[3], 13),
            mul(a[0], 13) ^ mul(a[1], 9) ^ mul(a[2], 14) ^ mul(a[3], 11),
            mul(a[0], 11) ^ mul(a[1], 13) ^ mul(a[2], 9) ^ mul(a[3], 14)
        ]
    return out

def aes_decrypt(ct_hex, key):
    state = hex_to_bytes(ct_hex)
    round_keys = key_expansion(key)
    
    print("\nInitial Cipher State:", bytes_to_hex(state))
    
    # Initial Round (Round 10 in encryption)
    state = add_round_key(state, round_keys[160:176])
    print("Round 10:", bytes_to_hex(state))
    
    # Rounds 9 down to 1
    for r in range(9, 0, -1):
        state = inv_shift_rows(state)
        state = inv_sub_bytes(state)
        state = add_round_key(state, round_keys[r*16:(r+1)*16])
        state = inv_mix_columns(state)
        print(f"Round {r}:", bytes_to_hex(state))
        
    # Final Round (Round 0)
    state = inv_shift_rows(state)
    state = inv_sub_bytes(state)
    state = add_round_key(state, round_keys[:16])
    print("Round 0:", bytes_to_hex(state))
    
    return bytes(state).decode('latin-1').rstrip()

# Socket Setup
try:
    sv = socket.socket()
    sv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1) # Allows restart without port timeout
    sv.bind(("0.0.0.0", 5009))
    sv.listen(1)
    print("Listening for incoming connection on port 5009...")
    
    c, addr = sv.accept()
    print("Connection accepted from", addr)
    
    ct = c.recv(1024).decode()
    print("Received Cipher Text:", ct)
    
    key_input = input("Enter Key: ").ljust(16)[:16].encode()
    pt = aes_decrypt(ct, key_input)
    
    print("\nDecryption complete")
    print("Text after decryption:", pt)

except Exception as e:
    print(f"An error occurred: {e}")
finally:
    if 'c' in locals(): c.close()
    if 'sv' in locals(): sv.close()
    print("Socket closed")
